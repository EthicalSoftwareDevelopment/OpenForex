# frontend/components

Purpose: Reusable React components for Deno-based frontend (charts, controls, widgets).
Suggested initial files:
- Button/, Chart/, TradeForm/ component folders
- index.tsx exporting shared components

Notes: Keep components small, well-documented and with unit/display tests.
