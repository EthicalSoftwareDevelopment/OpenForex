# Core Backend Configuration
# Add execution rules, portfolio settings, and other core parameters here.
#
# All modules should be configurable here. Reference shared schemas in ../../shared/schemas for data model validation.

# Example:
# order.matching.strategy=default
# execution.latency.ms=100

# Portfolio Tracking
# portfolio.tracking.enabled=true
# portfolio.tracking.schema=../../shared/schemas/portfolio.schema.json

# Leverage/Margin
# leverage.max=10
# margin.requirement=0.1

# Volatility Model
volatility.model=basic
volatility.window.size=30

# Historical Replay
replay.dataset=sample_data.csv
replay.speed=1.0

# Real-Time Price Feed
feed.source=synthetic
feed.frequency.ms=1000
