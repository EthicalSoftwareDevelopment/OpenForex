# backend/core

Purpose: Core trading mechanics (order matching, execution rules, portfolio tracking).
Suggested initial files:
- src/main/java/... (Java EE modules)
- README for module design and API contracts

Notes: Implement as a Maven/Gradle submodule if splitting into independent deployables.
