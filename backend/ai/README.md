# backend/ai

Purpose: AI-driven scenario generation and analytics (scenario models, training harness, inference endpoints).
Suggested initial files:
- models/ (training and model definitions)
- inference/ (serving code and API endpoints)

Notes: Keep heavy ML training pipelines separate from real-time inference services.
