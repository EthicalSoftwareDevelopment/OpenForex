# tests/backend

Purpose: Backend test suites (unit, integration, simulation regression tests).
Suggested initial files:
- unit/
- integration/

Notes: Use CI to run backend tests on JDK + containerized services if needed.
