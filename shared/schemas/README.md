# shared/schemas

Purpose: Shared data models (JSON schemas, OpenAPI fragments, TypeScript interfaces used by both backend and frontend).
Suggested initial files:
- price.schema.json
- order.schema.json

Notes: Keep canonical definitions here and generate language-specific DTOs during build where possible.
