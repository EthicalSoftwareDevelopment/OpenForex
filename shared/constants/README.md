# shared/constants

Purpose: Shared constants and configuration defaults (e.g., supported symbols, timeframes, default risk params).
Suggested initial files:
- symbols.json
- timeframes.json

Notes: Do not store secrets here; use environment-specific config for keys.
